package core

import (
	//"github.com/Rahul6700/load-balancer/models"
	"github.com/gin-gonic/gin"
)

func ToServer (c* gin.Context) {

	//param := c.Param()
	//method := c.Request.Method
	
	//selecting the target server
	// target := models.SelectServer()

}
